head
    meta(charset='utf-8')
    title Title2
    link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.2/css/bootstrap.min.css')

    body
        .modal-dialog
            .modal-content
                // Modal Header
                .modal-header
                    button.close(type="button", data-dismiss="modal")
                        span(aria-hidden="true") ×
                        span.sr-only Close
                    h4#myModalLabel.modal-title
                        | Edit Resource
                // Modal Body
                .modal-body
                    //#editForm.form(role='form' method='GET' action='https://www.google.com/')
                    form(role='form' method='GET' action='https://www.google.com/')
                        div.form-group
                            label(for='name') Title:
                            input#name.form-control(type='text', placeholder='Title' name='name')
                        div.form-group
                            label(for='authors') Authors:
                            input#authors.form-control(type='text', placeholder='Somebody' name='authors')
                        div.form-group
                            label(for='date') Pick date:
                            input#date.form-control(type='date', name='date')

                        div.form-switch
                            label Choose tags:
                            br
                            each tag in tags
                                div
                                    label.form-check
                                        input#chek(type="checkbox")
                                        .v-label #{tag.name}

                        button.btn.btn-primary(type="submit", name="asdf") Submit
                        //button.btn.btn-default(type="button", data-dismiss="modal")
                        //    | Close

                // Modal Footer
                .modal-footer
                    h4 footer